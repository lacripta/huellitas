<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("gd_grupo/index", "Go Back")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("gd_grupo/new", "Create ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Grupo</th>
            <th>Grupo Of Nombre</th>
            <th>Grupo Of Estado</th>
            <th>Grupo Of Fecha</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $gd_grupo): ?>
            <tr>
                <td><?php echo $gd_grupo->grupo_id ?></td>
            <td><?php echo $gd_grupo->grupo_nombre ?></td>
            <td><?php echo $gd_grupo->grupo_estado ?></td>
            <td><?php echo $gd_grupo->grupo_fecha ?></td>

                <td><?php echo $this->tag->linkTo(array("gd_grupo/edit/" . $gd_grupo->grupo_id, "Edit")); ?></td>
                <td><?php echo $this->tag->linkTo(array("gd_grupo/delete/" . $gd_grupo->grupo_id, "Delete")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("gd_grupo/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("gd_grupo/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("gd_grupo/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("gd_grupo/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
